type Achievement {
    id: ID!
    text: String!
    _id: ID!
    title: String!
    continuous: Boolean!
    caption: String
    star: Int
    target_point: Int
    point: Int
    updated_at: String!
}

type Query {
    achievements: [Achievement]

}

type Mutation {
    addAchievement(title: String!,continuous : Boolean! ,file : Upload): Achievement
}