
type PlayerLevel {
    _id: ID!
    level: Int!
    exp_req: Int!
    updated_at :String!
}

extend type Query {
    playerLevels: [PlayerLevel]
}

extend type Mutation {
    addPlayerLevel(level:Int!,exp_req:Int!):PlayerLevel
}
